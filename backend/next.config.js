/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you want to use the frontend code in Next.js, uncomment:
  // You can move frontend/src to backend/src/app or backend/src/pages
}

module.exports = nextConfig

